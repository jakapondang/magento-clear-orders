# Remove all orders and customers

SQL to remove all customers and orders from Magento

### Usage

Checkout the file and run the following command:

`mysql -u username -p -h localhost table_name < clearorders.sql`
